{
  "compilerOptions": {
    // JS API 类型定义：https://www.typescriptlang.org/tsconfig/#lib
    "lib": [
      "DOM", "DOM.Iterable",
      "ESNext"
    ],
    // 决定哪些 JS 特性会被降级处理，低于该版本的特性会被转译以兼容目标环境：https://www.typescriptlang.org/tsconfig/#target
    "target": "ESNext",
    "typeRoots": [
      "node_modules/@types",
      // "node_modules/@types/webpack-env",
      "utils/src/types"
    ],
    // 使用 "./" 时，TS 将从与 tsconfig.json 相同的文件夹开始查找文件：https://www.typescriptlang.org/tsconfig/#baseUrl
    "baseUrl": "./",
    // 同时需要在 webpack.config.js 中配置 resolve.alias
    "paths": {
      "@utils/*": [
        "utils/src/*"
      ]
    }
  }
}
